from __future__ import annotations


def anthro_pack_notes() -> dict[str, str]:
    return {
        "pack-layout": "Adopt lightweight skill metadata with deeper references, scripts and assets loaded only on demand.",
        "progressive-disclosure": "Do not front-load large doctrine blocks; select the pack first, then open references only when needed.",
        "phase-packs": "Treat attack, reverse, audit and evasion as dedicated skill-pack families rather than one monolithic prompt.",
    }
